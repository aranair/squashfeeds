<%= render 'back_team_standings' %>
<h1><%= @team.name %></h1>
</br>
<%= link_to 'Delete Team', team_path(@team), method: 'DELETE', class: 'btn btn-default', data: { confirm: "Are you sure?" }  %>
<%= link_to 'Back', teams_path, class: 'btn btn-default'  %>
</br></br>
<table class="table">
  <tbody>
    <%- Grade.all.each do |grade| %>
      <tr>
        <td>Points <%= grade.name %></td>
        <td><%= @team.send("points_#{grade.key}") %></td>
      </tr>
    <%- end %>
  </tbody>
</table>
