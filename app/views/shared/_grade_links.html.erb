<%- Grade.keys.each do |grade| %>
  <%= render_grade_link(grade) %>
  <%- if grade == 'f' %>
    </br></br>
  <%- end %>
<%- end %>
<%- if !local_assigns.has_key?(:skip_clear) && @grade.present? -%>
  <%= link_to 'Clear', params.clear, class: 'btn btn-default' %>
<%- end %>
