<%- Grade.keys.each do |grade| %>
  <%= render_grade_link(grade) %>
  <%- if grade == 'f' %>
    </br></br>
  <%- end %>
<%- end %>
<%= link_to 'Clear', params.clear, class: 'btn btn-default' if local_assigns.has_key?(:skip_clear) %>
